import { useState } from 'react';

/**
 * Component for showing each Item, location, time, length, user, and when they're available. 
 * These cards make up the schedule slider and can probably be used elsewhere.
 * 
 * Parent: ScheduleSlider
 * Location: Home Page
 * 
 * Props: 
 * - Username
 * - Timezone
 * - Times Available
 * 
 */

export default function ScheduleCard({ username, timezone, availableTimes }) { // TODO Replace these props with proper names when we get the data model figured out

  return (
    <>
      { /* TODO Add ScheduleCard JSX */}
    </>
  );
}